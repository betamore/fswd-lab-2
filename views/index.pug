html
    head
        title Hello world!
    body
        p This is the #[code index.pug] template

        form(action="/somepath" method="POST")
            p Enter your name!
            input(type="text" name="name")
            button(type="submit") Click me!
        